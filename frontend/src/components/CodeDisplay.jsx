import React from 'react'
import { useTheme } from '../App'

function CodeDisplay({ code }) {
 
  

  return (
    <div className={`h-full ${isDarkMode ? 'bg-black' : 'bg-white'} p-4`}>
      
    </div>
  )
}

export default CodeDisplay
